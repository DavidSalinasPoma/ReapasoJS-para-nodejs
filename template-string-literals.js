const nombre = 'Deapol';
const real = 'Wade Winston';

// Concatenacion normal de cadenas
const normal = nombre + ' ' + real;

// Mostrar la concatenación
console.log(normal);

// Concatenación con template string literal
const template = `${nombre} ${real}`;

// Mostrar concatenacion
console.log(template);
